# custom.yml is autogenerated by scripts/ci_generator.py do not edit by hand.
on:
- push
- pull_request
- pull_request_target
jobs:
  ci_configure_self_test:
    name: ci_configure_self_test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - working-directory: .
      run: sudo apt-get install python3 && pip3 install ruamel.yaml
      name: Install
    - working-directory: .
      run: python3 ./scripts/ci_generator.py
      name: Build
    - working-directory: .
      run: echo if you fail this test it means you did not autogenerate and pushed
        the generated files && git diff && git diff-index --name-status --exit-code
        HEAD
      name: Test
  whitespace:
    name: whitespace
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - working-directory: .
      run: git diff-tree --check origin/master HEAD
      name: Test
  cppcheck:
    name: cppcheck
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - working-directory: .
      run: sudo apt-get install cppcheck
      name: Install
    - working-directory: .
      run: cppcheck . --force --std=c99
      name: Test
name: Custom Build Matrix
